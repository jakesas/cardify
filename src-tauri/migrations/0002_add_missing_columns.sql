-- Migration v2: No-op. All columns (tag, code_snippet, topology, reps) 
-- were already defined in the v1 init schema (0001_init.sql).
-- This migration is intentionally left empty to preserve the migration version history.
SELECT 1;
